apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: graph-viewer
  namespace: dependencies
spec:
  path: /
  to:
    kind: Service
    name: graph-viewer-service
  port:
    targetPort: 4200