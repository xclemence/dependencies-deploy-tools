apiVersion: v1
kind: Service
metadata:
  name: graph-viewer-service
  namespace: dependencies
spec:
  selector:
    app: dependencies-graph-viewer
  ports:
    - protocol: TCP
      name: http
      port: 4200
      targetPort: 4200

