apiVersion: v1
kind: Service
metadata:
  name: graph-graphql-service
  namespace: dependencies
spec:
  selector:
    app: dependencies-graph-graphql
  ports:
    - protocol: TCP
      name: http
      port: 4001
      targetPort: 4001

